{
  "name": "tzblog",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.29.2",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:web": "pnpm --filter web dev",
    "dev:cms": "pnpm --filter cms dev",
    "local:start": "bash scripts/local-dev.sh start",
    "local:web": "bash scripts/local-dev.sh web",
    "local:cms": "bash scripts/local-dev.sh cms",
    "local:stop": "bash scripts/local-dev.sh stop",
    "local:restart": "bash scripts/local-dev.sh restart",
    "local:status": "bash scripts/local-dev.sh status",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:up": "docker compose -f infra/docker-compose.yml up -d",
    "db:down": "docker compose -f infra/docker-compose.yml down",
    "db:logs": "docker compose -f infra/docker-compose.yml logs -f postgres"
  },
  "devDependencies": {
    "turbo": "2.8.21"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp",
      "esbuild",
      "unrs-resolver"
    ]
  }
}
