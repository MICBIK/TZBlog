# /project:update-context

根据本次对话完成的工作，更新以下文件：

1. memory-bank/activeContext.md（当前焦点、关键决策、下一步、阻塞项）
2. memory-bank/progress.md（勾选完成项、新增待办、技术债务）
3. 如果 openspec/config.yaml 的 context 与 memory-bank 有差异，一并同步

完成后说："上下文已更新，当前状态：[一句话摘要]"
